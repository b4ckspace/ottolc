#pragma once
enum Setting{
  trimLeftFoot,
  trimRightFoot,
  trimLeftLeg,
  trimRightLeg,
  trimChecksum,
};

// enum for servo->number in servos array
enum EServo{
	rightFoot=0,
	leftFoot,
	rightLeg,
	leftLeg,
};